# The Books. Pages serves /docs from main directly, so this job only has to
# regenerate the page when the event log changes and commit the result.
name: The Books

on:
  push:
    branches: [main]
    paths:
      - "docs/events.jsonl"
      - "grumpysenior/grumpy/dashboard.py"
      - "grumpysenior/grumpy/metrics.py"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: the-books
  cancel-in-progress: true

jobs:
  rebuild:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: { python-version: "3.12" }

      - run: pip install --quiet ./grumpysenior

      # Every record is anonymous by construction: an install id, which models
      # sat, what they agreed on, what it cost. Nothing about anyone's code is
      # in there — which is the only reason this page is allowed to be public.
      - run: grumpy dashboard --log docs/events.jsonl --out docs/index.html

      - name: Commit if the numbers moved
        run: |
          if git diff --quiet docs/index.html; then
            echo "The Books are unchanged."
            exit 0
          fi
          git config user.name  "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add docs/index.html
          git commit -m "The Books: rebuild from the event log"
          git push
